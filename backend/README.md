# Backend

## Environment setup

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Testing
You need to be in `origuide` directory.

```bash
python -m unittest
```